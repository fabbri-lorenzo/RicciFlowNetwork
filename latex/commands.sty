%%% To remove rectangles around references
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    filecolor=black,      
    urlcolor=black,
    citecolor=black,
    pdfpagemode=FullScreen}
    
\pgfplotsset{compat=1.18} 

\contourlength{1.2pt}

\DeclareMathOperator{\Tr}{Tr}

\newcommand{\calI}{\mathscr{I}} %\mathcal

%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%TIKZ
\usetikzlibrary{math,external, decorations.markings,decorations.pathmorphing, arrows.meta, angles,quotes, calc}

\tikzset{>=latex} % for LaTeX arrow head

\tikzset{make origin horizontal center of bounding box/.style={%
execute at end picture={%
\path let \p1=(current bounding box.west),\p2=(current bounding box.east)
in ({-max(-1*\x1,\x2)},\y1) ({max(-1*\x1,\x2)},\y1);
}}}%to centre tikzpictures


%%%%%%%%TEXTstuff
\textwidth=450pt\oddsidemargin=0pt

\makeatletter

\newcommand\frontmatter{%
    \cleardoublepage
  %\@mainmatterfalse
  \pagenumbering{Roman}}

\newcommand\mainmatter{%
    \cleardoublepage
 % \@mainmattertrue
  \pagenumbering{arabic}}

\newcommand\backmatter{%
  \if@openright
    \cleardoublepage
  \else
    \clearpage
  \fi
 % \@mainmatterfalse
   }

\makeatother

\setlength{\headheight}{14.49998pt}